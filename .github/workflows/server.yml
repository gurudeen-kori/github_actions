server:
  name: my-server
  ip: 192.168.1.10
  port: 8080

database:
  host: localhost
  name: mydb
  credentials:
    user: admin
    password: secret123

startup_script_literal: |
  #!/bin/bash
  echo "Starting server..."
  sudo systemctl start nginx
  echo "Server started"

startup_script_folded: >
  #!/bin/bash
  echo "Starting server..."
  sudo systemctl start nginx
  echo "Server started"
