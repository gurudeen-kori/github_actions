name: deploy app

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    env:
      DOCKER_USER: ${{ vars.DOCKER_USER }}
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Deploy app
        run: docker compose up -d --build